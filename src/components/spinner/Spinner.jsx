import { ContainerSpinner, Spin } from "../../styles/spinner/Spinner";

const Spinner = () => {
  return (
    <ContainerSpinner>
      <Spin />
    </ContainerSpinner>
  );
};

export default Spinner;
